## 已确认事实清单（TinyEMU + Linux 6.12 启动调试）v10

### 一、硬件/平台层（TinyEMU 模拟器）

1. TinyEMU 的 ISA：`rv64imafdc`，不支持 Zba/Zbb/Zbc/V 等新扩展
2. RAM 起始地址：`0x80000000`
3. HTIF MMIO 基地址：`0x40008000`，布局：offset 0 = tohost，offset 8 = fromhost
4. CLINT 地址：`0x02000000`，PLIC 地址：`0x40100000` — 与 OpenSBI generic 平台天然匹配
5. 串口输出唯一通道：HTIF（`compatible = "ucb,htif0"`），没有 ns16550 / sifive-uart
6. TinyEMU 创建了 virtio console 设备（`virtio_console_init(vbus, p->console)`）

### 二、OpenSBI 层（v1.6，Buildroot 编译）

7. OpenSBI generic 平台已编译 HTIF 串口驱动（`fdt_serial_htif.c` 在 carray 中）
8. OpenSBI banner 能完整输出 — HTIF 通路正常（运行态确认）
9. `Platform Console Device: htif` — OpenSBI 正确识别了 HTIF（运行态确认）
10. fw_payload 模式可用（OpenSBI + 内核打包为一个 `fw_payload.bin`，当 bios 加载）
11. OpenSBI 的 HTIF FDT 约定：reg[0] = fromhost 地址，reg[1] = tohost 地址（与直觉相反）
12. 当前 FDT 中 htif 节点的 reg 值是正确的：`reg[0]=0x40008008`(fromhost), `reg[1]=0x40008000`(tohost)（riscv_machine.c:659）

### 三、TinyEMU C 代码已做的修复（当前仓库实际状态）

13. FDT chosen 节点已加 `stdout-path = "/htif"`（riscv_machine.c:734）
14. FDT htif 节点已加 `reg` 属性（riscv_machine.c:659）
15. `htif_handle_cmd()` 的 else 分支已加 `s->htif_tohost = 0`（riscv_machine.c:151）
16. CSR `0xf11`(mvendorid)、`0xf12`(marchid)、`0xf13`(mimpid) 已补实现，返回 0（riscv_cpu.c:844）
17. CSR stub 列表（读返回 0 / 写忽略）：pmpcfg0/2 (0x3a0/0x3a2)、mcountinhibit (0x320)、mhpmcounter3-31 (0xb03-0xb1f)、mhpmevent3 (0x321)、pmpaddr0-15 (0x3b0-0x3bf)
18. `COUNTEREN_MASK` 已包含 TM 位（riscv_cpu.c:648）
19. `rtc_get_time()` 的 `!rtc_real_time` 分支直接返回 `riscv_cpu_get_cycles()`，**不除以 RTC_FREQ_DIV**（riscv_machine.c:91）。`RTC_FREQ_DIV=16` 仍定义但未参与计时路径（:80）
20. `riscv_machine_get_sleep_duration()` 中 timecmp 比较已改为 `uint64_t` 无符号比较
21. `RISCVCPUState` 新增 `uint64_t *timecmp_ptr` 字段（riscv_cpu_priv.h:239），machine 初始化时指向 `RISCVMachine.timecmp`（riscv_machine.c:920）
22. `riscv_cpu_template.h` 的 inline timer 检查：`insn_counter >= *timecmp_ptr`，**不除以 16**（riscv_cpu_template.h:252）
23. 新增 `riscv_cpu_set_timecmp_ptr()` setter 函数
24. `htif_poll()` 已启用且每 tick 调用（riscv_machine.c:182, :1107）
25. **menvcfg(0x30a)、stimecmp(0x14d)、Sstc/STIP inline 检查 — 当前代码中不存在**（v9 清单中的 #25-#28 是之前的改动，已被回滚）

### 四、TinyEMU C++ 代码已做的修复

26. `CharacterDevice` 已从栈上局部变量提升为类成员 `_console_dev_storage`（tinyemu_vm.h:72）
27. `p.rtc_real_time = false`（tinyemu_vm.cpp:590）
28. `_exec_cycles_per_tick = 1 << 20`（tinyemu_vm.h:84）

### 五、内核层（Linux 6.12.20，Buildroot 编译）

29. 内核确实在运行 — initcall 序列正常推进（运行态确认）
30. `CONFIG_HVC_RISCV_SBI=y` 已编译
31. **当前 fw_payload.bin 内嵌的内核 CMDLINE 是 `console=hvc0 root=/dev/vda rw`，没有 `earlycon=sbi`**（Codex 从二进制中确认）
32. `CONFIG_VIRTIO_CONSOLE=y`、`CONFIG_VIRTIO_BLK=y`、`CONFIG_VIRTIO_NET=y` 已有
33. 内核使用 Buildroot 的 `qemu_riscv64_virt_defconfig` 为基础
34. `CONFIG_RISCV_EMULATED_UNALIGNED_ACCESS=y`
35. `CONFIG_HW_RANDOM=y` + `CONFIG_HW_RANDOM_VIRTIO=y`

### 六、已解决的问题

36. ~~OpenSBI banner 后内核无输出~~ — 已解决：CMDLINE 加 `earlycon=sbi`
37. ~~内核卡在 `time_init()`~~ — 已解决：timecmp 溢出导致中断风暴
38. ~~内核卡在 `check_unaligned_access_all_cpus`~~ — 已解决（绕过）：`CONFIG_RISCV_EMULATED_UNALIGNED_ACCESS=y`
39. ~~S-mode timer 中断未到达内核~~ — 已解决：CSR `0xfb0`(mtopi) stub 导致 OpenSBI 误判 SMAIA
40. 从 stub 列表删除的危险 CSR：`0x14d`、`0x30a`、`0x30c`、`0x7a0`、`0x7a1`、`0x7a4`、`0xda0`、`0xfb0`
41. ~~输入 root 后虚拟机重启~~ — 已解决：关闭 virtio-net
42. ~~login 无回显~~ — 已解决：`htif_poll()` 启用

### 七、时钟源现状（关键修正）

43. CSR time (0xc01) 返回 `insn_counter`
44. `rtc_get_time()` 返回 `riscv_cpu_get_cycles()` = `insn_counter`（不除以 16）
45. CLINT mtime MMIO 读调用 `rtc_get_time()`，因此也返回 `insn_counter`
46. **当前 CSR time、CLINT mtime、inline timer check 三者一致，都基于 `insn_counter` 原始值**（v9 中关于 /16 不一致的描述已不成立）
47. FDT 声明 `timebase-frequency = 10000000`（10MHz），但实际 mtime 以 insn_counter 速率递增
48. `RTC_FREQ_DIV=16` 仍定义但未使用
49. `riscv_cpu_get_cycles()` 返回 `s->insn_counter`（riscv_cpu.c:1328）

### 八、CLINT / timer 机制备忘

50. timer 调用链：内核 SBI ecall → OpenSBI 写 CLINT mtimecmp → `insn_counter >= timecmp` 时 set MIP_MTIP → OpenSBI 捕获 → `sbi_timer_process()` → set MIP_STIP → 内核收到 S-mode timer 中断
51. CLINT MMIO 写 timecmp 时会 `riscv_cpu_reset_mip(MIP_MTIP)` 清除 timer pending
52. TinyEMU 没有内嵌 SBI 实现，ecall 由 OpenSBI 固件在 M-mode 处理

### 九、Console 通路

53. HTIF 输出：内核 → SBI ecall → OpenSBI → HTIF → TinyEMU → Godot
54. HTIF 输入：Godot → TinyEMU `htif_poll()` → HTIF → OpenSBI → 内核 `hvc_riscv_sbi` → TTY
55. `hvc_riscv_sbi` 输入是轮询机制
56. virtio console 设备同时存在，`CONFIG_VIRTIO_CONSOLE=y`

### 十、virtio-rng 设备

57. 已实现，guest 中 `/dev/hwrng` 可读，crng init done
58. 熵不足假说已排除

### 十一、用户态程序卡死问题 — 根因已定位

59. bc 正常（busybox 静态链接）
60. nano、curl、python3.12、file、strace 卡死
61. **`read()` 超过 128KB (131072 字节) 时卡死**
62. 精确边界：128KB 成功，130KB 卡死 — 128KB = 32 × 4KB 页面
63. 老内核 4.15 不存在此问题
64. **TLB_SIZE = 256**（riscv_cpu_priv.h:101）— v9 中"TLB 通常 32 entry"的假说不成立，需要重新分析根因
65. 动态链接器能列出依赖但直接执行卡死
66. 简单测试程序（hello/malloc/ioctl/pthread/mmap/poll）均正常

### 十二、构建环境备忘

67. Buildroot 路径：`~/.cache/jediterm_tinyemu_buildroot/buildroot-2025.02.1`
68. WSL2 编译前必须 `export PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin`
69. fw_payload 需要手动编译
70. 产物复制目标：`/mnt/e/development/JediTerm-Godot/addons/jediterm/native/tinyemu/images/python/fw_payload.bin`
71. TinyEMU DLL 位置：`addons/jediterm/bin/win64/`

### 十三、调试代码待清理

72. riscv_cpu.c 中 `set_mip` / `raise_exc` / `mcause_read` 有 `[TIMER_DBG]` printf
73. 内核源码中可能仍有手工 DEBUG pr_err（需运行态确认）

### 十四、关键待解决问题

74. **（最高优先级）read() >128KB 卡死**：TLB_SIZE=256 排除了"32 entry TLB 抖动"假说，需要重新分析。可能方向：
    - Linux 6.12 的 `copy_to_user` 在大块拷贝时的页表操作（batched TLB flush / `sfence.vma`）
    - TinyEMU 的 `sfence.vma` 实现是否正确处理了 ASID / 地址范围参数
    - 对比 Linux 4.15 和 6.12 的 `arch/riscv/lib/uaccess.S`
75. **earlycon=sbi 缺失**：当前 fw_payload.bin 内嵌 CMDLINE 没有 `earlycon=sbi`，如果系统能启动说明 OpenSBI 可能通过其他方式初始化了 earlycon，或者当前跑的是之前编译的旧产物
76. virtio-net TX timeout — 通过 `network: false` 规避中
77. 调试代码清理


PS:记住你操作文件，只能通过我。。。在wsl2里面通过sed这类的东西来搞
